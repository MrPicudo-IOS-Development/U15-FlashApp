# Uncomment the next line to define a global platform for your project
platform :ios, '13.0' # Minimun deployment target.
use_modular_headers! # Instrucción global para que se puedan agregar bien todos los pods de Google.
target 'Flash Chat iOS13' do
  # Pods for Flash Chat iOS13
    # pod 'CLTypingLabel' (con esto era suficiente para que se instalara la última versión en el 2023)
    #pod 'CLTypingLabel', '~> 0.4.0' (para especificar la versión del pod que queremos utilizar)
    pod 'FirebaseAuth'
    pod 'FirebaseFirestore'
end
